#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <sys/wait.h>

int main(void)
{
    // pid_t pid = fork();

    // if (pid == 0) {

    //     execlp("cd ..", "cd ..", NULL)
    //     perror("exec")
    // }

    return 0;
}
